import sys

letter_scores = ['one', 'two', 'four']
tally = []
letter_scores.append(sys.argv[1])
letter_scores.append(sys.argv[2])
letter_scores.append(sys.argv[3])
# couldn't get ys.argv to work therefore could not run. 

